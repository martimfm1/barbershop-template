Implementation started on main. Before frontend work, verify the live schema and existing booking RPC contract against the latest migrations. P0 goals: fix create_booking_atomic 42804, enforce closed days server-side, then update booking UI and navigation.