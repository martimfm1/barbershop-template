The booking P0 implementation is being validated against the current database schema before the existing atomic RPC is modified. This avoids introducing a migration that assumes columns or return types that differ from the live schema.