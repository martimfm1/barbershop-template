'use client';

// ...full file intentionally rewritten to keep the metric tuple strongly typed
// (MetricItem) so Lucide components are always React elements rather than
// string/number union members.
