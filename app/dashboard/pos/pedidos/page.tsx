'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Check, ClipboardList, Clock3, PackageCheck, Search, XCircle } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { toast } from 'sonner';

type Item = { id: string; product_name: string; quantity: number; unit_price: number; total: number };
type Order = { id: string; customer_name: string; customer_email: string; customer_phone: string; notes: string | null; subtotal: number; total: number; status: 'pending'|'confirmed'|'ready'|'completed'|'cancelled'; created_at: string; marketplace_order_items: Item[] };
const money=(n:number)=>new Intl.NumberFormat('pt-PT',{style:'currency',currency:'EUR'}).format(Number(n)||0);
const statusMeta={
 pending:{label:'Novo',icon:Clock3,classes:'bg-amber-400/10 text-amber-300 border-amber-400/20'},
 confirmed:{label:'Confirmado',icon:Check,classes:'bg-sky-400/10 text-sky-300 border-sky-400/20'},
 ready:{label:'Pronto',icon:PackageCheck,classes:'bg-emerald-400/10 text-emerald-300 border-emerald-400/20'},
 completed:{label:'Levantado',icon:ClipboardList,classes:'bg-zinc-400/10 text-zinc-300 border-zinc-400/20'},
 cancelled:{label:'Cancelado',icon:XCircle,classes:'bg-rose-400/10 text-rose-300 border-rose-400/20'},
} as const;

export default function MarketplaceOrdersPage(){
 const [orders,setOrders]=useState<Order[]>([]); const [loading,setLoading]=useState(true); const [query,setQuery]=useState(''); const [status,setStatus]=useState('all'); const [updating,setUpdating]=useState<string|null>(null);
 async function load(){setLoading(true);try{const r=await fetch('/api/marketplace/orders',{cache:'no-store'});const j=await r.json();if(!r.ok)throw new Error(j.error||'Não foi possível carregar os pedidos.');setOrders(j.orders??[])}catch(e){toast.error(e instanceof Error?e.message:'Erro ao carregar pedidos.')}finally{setLoading(false)}}
 useEffect(()=>{void load()},[]);
 const filtered=useMemo(()=>orders.filter(o=>(status==='all'||o.status===status)&&(!query||`${o.id} ${o.customer_name} ${o.customer_email} ${o.customer_phone}`.toLocaleLowerCase('pt-PT').includes(query.toLocaleLowerCase('pt-PT')))),[orders,status,query]);
 async function changeStatus(order:Order,next:Order['status']){setUpdating(order.id);try{const r=await fetch(`/api/marketplace/orders/${order.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:next})});const j=await r.json();if(!r.ok)throw new Error(j.error||'Não foi possível atualizar.');setOrders(c=>c.map(x=>x.id===order.id?{...x,status:next}:x));toast.success('Pedido atualizado.')}catch(e){toast.error(e instanceof Error?e.message:'Erro ao atualizar.')}finally{setUpdating(null)}}
 return <main className="min-h-screen bg-zinc-950 px-4 py-8 text-zinc-100 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><Link href="/dashboard/pos" className="inline-flex min-h-10 items-center gap-2 text-sm text-zinc-500 hover:text-white"><ArrowLeft className="size-4"/>Voltar ao ponto de venda</Link><p className="mt-5 text-xs font-semibold uppercase tracking-[0.18em] text-emerald-400">Marketplace</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Encomendas</h1><p className="mt-2 text-sm text-zinc-500">Recebe, confirma e prepara os pedidos feitos pelos clientes.</p></div><Button variant="outline" onClick={()=>void load()}>Atualizar</Button></header>
 <Card className="border-white/10 bg-white/[0.02]"><CardContent className="p-3"><div className="grid gap-3 md:grid-cols-[1fr_auto]"><div className="relative"><Search className="pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-zinc-600"/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Pesquisar nome, email ou pedido" className="min-h-11 border-white/10 bg-white/[0.03] pl-9" aria-label="Pesquisar encomendas"/></div><select value={status} onChange={e=>setStatus(e.target.value)} aria-label="Filtrar estado" className="min-h-11 rounded-xl border border-white/10 bg-zinc-950 px-3 text-sm"><option value="all">Todos os estados</option><option value="pending">Novos</option><option value="confirmed">Confirmados</option><option value="ready">Prontos</option><option value="completed">Levantados</option><option value="cancelled">Cancelados</option></select></div></CardContent></Card>
 {loading?<div className="grid gap-4">{Array.from({length:4}).map((_,i)=><div key={i} className="h-40 animate-pulse rounded-3xl bg-white/[0.03]"/>)}</div>:filtered.length===0?<div className="rounded-3xl border border-dashed border-white/10 p-14 text-center text-sm text-zinc-600">Não há encomendas com estes filtros.</div>:<div className="space-y-4">{filtered.map(order=>{const meta=statusMeta[order.status];const Icon=meta.icon;const next=order.status==='pending'?'confirmed':order.status==='confirmed'?'ready':order.status==='ready'?'completed':null;return <Card key={order.id} className="overflow-hidden border-white/10 bg-white/[0.02]"><CardHeader className="border-b border-white/[0.06] p-4 sm:p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2"><span className={`flex size-8 items-center justify-center rounded-lg border ${meta.classes}`}><Icon className="size-4"/></span><CardTitle className="text-base">{order.customer_name}</CardTitle></div><p className="mt-2 text-xs text-zinc-500">Pedido <span className="font-mono text-zinc-400">{order.id.slice(0,8)}</span> · {new Date(order.created_at).toLocaleString('pt-PT')}</p></div><span className={`w-fit rounded-full border px-2.5 py-1 text-[11px] font-semibold ${meta.classes}`}>{meta.label}</span></div></CardHeader><CardContent className="space-y-4 p-4 sm:p-5"><div className="grid gap-3 lg:grid-cols-[1fr_240px]"><div className="space-y-2">{order.marketplace_order_items.map(item=><div key={item.id} className="flex items-center justify-between gap-3 rounded-xl border border-white/[0.055] bg-white/[0.02] p-3 text-sm"><span className="min-w-0 truncate"><strong>{item.product_name}</strong><span className="ml-2 text-xs text-zinc-600">× {item.quantity}</span></span><span className="font-semibold">{money(item.total)}</span></div>)}{order.notes&&<div className="rounded-xl border border-amber-400/10 bg-amber-400/[0.03] p-3 text-xs text-amber-100"><strong>Nota:</strong> {order.notes}</div>}</div><div className="rounded-2xl border border-white/10 bg-white/[0.025] p-4"><p className="text-xs text-zinc-500">Contacto</p><p className="mt-2 text-sm text-zinc-200">{order.customer_email}</p><p className="mt-1 text-sm text-zinc-400">{order.customer_phone}</p><div className="my-4 h-px bg-white/[0.06]"/><div className="flex items-center justify-between"><span className="text-sm text-zinc-500">Total</span><strong className="text-xl">{money(order.total)}</strong></div></div></div><div className="flex flex-col gap-2 sm:flex-row sm:justify-end">{next&&<Button disabled={updating===order.id} onClick={()=>void changeStatus(order,next)} className="min-h-11 bg-emerald-400 text-zinc-950 hover:bg-emerald-300">{next==='confirmed'?'Confirmar pedido':next==='ready'?'Marcar como pronto':'Marcar como levantado'}</Button>}{order.status!=='cancelled'&&order.status!=='completed'&&<Button variant="outline" disabled={updating===order.id} onClick={()=>void changeStatus(order,'cancelled')} className="min-h-11 text-rose-300">Cancelar pedido</Button>}</div></CardContent></Card>})}</div>}
 </div></main>
}
